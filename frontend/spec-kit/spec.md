# spec.md — 비즈니스 정의

> 무엇을 만드는가. 기능 요구사항을 정의한다.
> 구현 방법이 아닌 "무엇을 해야 하는가"에 집중한다.

---

## 서비스 개요

TFT(전략적 팀 전투) 전적 검색 서비스.
소환사 전적 조회, 현재 메타 스냅샷, 덱 추천, 게임 가이드, 패치 노트를 제공한다.

---

## 페이지별 기능 정의

### 1. Dashboard — 메인 (소환사 전적 검색)

- 소환사명#태그 형식으로 검색할 수 있다
- 최근 검색어 태그를 클릭해 빠르게 재검색할 수 있다
- 검색 결과로 소환사 카드가 표시된다
  - 소환사명, 태그, 티어, LP, 승수, 패수, 승률
- 현재 패치 메타 스냅샷을 종합 / 상위권 / 마스터+ 탭으로 확인할 수 있다
  - 순위, 티어 등급(S/A+/A), 덱 이름, 주요 유닛, 승률, 평균 등수
- 소환사를 검색하지 않아도 메타 스냅샷은 기본 노출된다

### 2. SummonerDetail — 소환사 전적 상세

- 소환사 카드를 확인할 수 있다
  - 소환사명, 태그, 티어, LP, 승수, 패수, 승률, 평균 순위, TOP4율
  - 순위 분포 바 차트 (1~8등별 게임 수)
- 전적 업데이트 버튼을 클릭해 최신 데이터로 갱신할 수 있다
- 많이 플레이한 시너지 / 챔피언 통계를 확인할 수 있다
  - 순위, 시너지(챔피언) 이미지, 이름, 게임 수, 평균 등수
- 최근 30게임 요약을 확인할 수 있다
  - 순방확률 도넛 차트 (TOP4 비율)
  - 순방확률: ~W ~L (~%)
  - 평균 순위: ~th / 8
- 최근 전적 목록을 확인할 수 있다
  - 순위 (1위~8위 형식)
  - 게임 유형 배지 (랭크 / 일반) — Riot API queue_id 기준 (1100=랭크, 1090=일반)
  - 덱 이름 (Spring 서버에서 시너지/유닛 조합을 분석해 자동 생성)
  - 게임 시간 (방금 전 / 몇 분 전 / 몇 시간 전 등)
  - LP 변동값은 제공하지 않는다 (Riot API 미제공)
  - 유닛 이미지 및 유닛별 아이템 이미지 (최대 3개)
  - 챔피언·아이템 이미지 호버 시 이름 툴팁 표시
- 전적 목록은 게임 유형(전체 / 랭크 / 일반)으로 필터링할 수 있다
- 전적 행 클릭 시 해당 게임의 8명 전원 상세 정보가 펼쳐진다
  - 순위, 소환사명, 스테이지(Riot API의 last_round를 Spring에서 스테이지 형식으로 변환, 예: 5 → 2-1), 시너지, 챔피언(아이템 포함), 킬수, 잔여골드
  - 순위 색상: 1위 금색, 2위 은색, 3위 동색, 4위 이하 기본색
  - 시너지 아이콘은 Community Dragon CDN 이미지를 사용하며, 소환사 카드의 "많이 플레이한 시너지" 섹션과 동일한 이미지 소스를 공유한다
  - 증강은 표시하지 않는다 (Riot API 공식 스키마 미포함 — 미제공 확정)
  - 내 행은 별도 하이라이트 표시
- 전적은 30개씩 표시하며, "30개 더 보기" 버튼으로 추가 로드할 수 있다
- 로그인 없이 누구나 검색 및 조회할 수 있다

#### 데이터 요구사항 (Riot API 기반)

- 소환사 정보는 다음 5개 Riot API를 조합해 Spring 서버에서 가공한 후 프론트에 단일 응답으로 내려준다
  - `GET /riot/account/v1/accounts/by-puuid/{puuid}` → gameName, tagLine (없을 수 있음, NULL 허용)
  - `GET /tft/summoner/v1/summoners/by-puuid/{puuid}` → profileIconId, summonerLevel
  - `GET /tft/league/v1/by-puuid/{puuid}` → tier, rank, leaguePoints, wins, losses
  - `GET /tft/match/v1/matches/by-puuid/{puuid}/ids` → 매치 ID 목록 (count 파라미터로 요청 수 조절)
  - `GET /tft/match/v1/matches/{matchId}` → 매치 상세 (참가자, 유닛, 시너지 등)
- 매치 데이터는 queue_id 1100(RANKED_TFT)과 1090(NORMAL_TFT)만 수집해 저장한다; 하이퍼롤 등 그 외 queue_id는 제외한다
- 게임 유형은 Spring에서 queue_id를 변환해 내려준다 (1100 → 랭크, 1090 → 일반)
- wins는 4위 이상(순방) 횟수, losses는 5~8위(4위 미만) 횟수이며, 승률(순방확률)은 wins / (wins + losses)로 계산한다
- 순위 분포(1~8등별 게임 수)는 매치 데이터의 placement를 집계해 Spring에서 계산한다
- 평균 순위, TOP4율은 Spring에서 최근 매치 데이터를 기반으로 계산한다
- 덱 이름은 Spring에서 해당 게임의 시너지/유닛 조합을 분석해 자동 생성한다
- 스테이지 표시는 Riot API의 last_round(int)를 Spring에서 변환한다 (예: 5 → 2-1)
- 시너지 등급(tone)은 Riot API의 style(0~4)을 Spring에서 변환한다 (0=없음, 1=브론즈, 2=실버, 3=골드, 4=크로매틱)
- 챔피언 별 수(stars)는 Riot API의 tier(int)를 그대로 사용한다
- 챔피언·아이템·시너지 이미지는 Community Dragon CDN을 사용하며 Spring에서 URL을 조합해 내려준다
- 증강(augments)은 Riot API 공식 스키마에 없으므로 제공하지 않는다 (UI에 표시하지 않음)

### 3. Decks — 덱 목록

- 현재 패치 추천 덱 목록을 확인할 수 있다
- 덱 클릭 시 상세 유닛 구성, 아이템, 포지셔닝을 확인할 수 있다

### 4. AiRecommend — AI 덱 추천

- AI가 현재 보유 유닛/아이템을 분석해 최적 덱을 추천한다
- 추천 덱은 유닛 구성, 아이템 배치, 포지셔닝을 포함한다

### 5. Guide — 게임 가이드

- 게임 가이드는 시너지, 아이템, 증강체, 챔피언 정보를 탭 단위로 탐색할 수 있어야 한다
- 각 가이드 탭은 키워드 검색을 제공해야 한다
- 검색어 또는 필터 변경 시 목록은 첫 페이지로 초기화되어야 한다
- 목록형 가이드는 페이지네이션을 제공해야 한다
  - 시너지 목록은 한 페이지에 6개씩 표시한다
  - 아이템 목록은 한 페이지에 5개씩 표시한다
  - 증강체 목록은 한 페이지에 5개씩 표시한다
  - 챔피언 목록은 한 페이지에 10개씩 표시한다
  - 페이지 번호는 한 번에 최대 5개까지만 표시하고 이후 페이지는 다음/더보기 흐름으로 이동한다

#### 시너지 가이드

- 시너지별 이름, 유형, 활성 단계, 효과 설명을 확인할 수 있어야 한다
- 각 시너지의 필요 챔피언 목록을 확인할 수 있어야 한다
- 각 시너지별 운영 팁 또는 주의점을 확인할 수 있어야 한다
- 시너지 카드에는 사용자가 직접 이해해야 하는 정보만 노출하고 불필요한 연동 대상 영역은 노출하지 않는다
- 시너지에 연결된 챔피언을 통해 챔피언 가이드로 이동할 수 있어야 한다

#### 아이템 통계

- 아이템별 승률, TOP4 비율, 픽률, 평균 등수를 확인할 수 있어야 한다
- 아이템별 추천 장착 챔피언을 확인할 수 있어야 한다
- 아이템 조합 추천과 조합별 사용 목적을 확인할 수 있어야 한다
- 아이템 목록은 승률, TOP4 비율, 픽률, 평균 등수 기준으로 정렬할 수 있어야 한다
- 아이템 통계는 향후 매치 데이터 기반으로 계산 가능한 구조여야 한다

#### 증강체 가이드

- 증강체별 티어, 승률, 픽률, 평균 등수, 설명을 확인할 수 있어야 한다
- 증강체는 티어표 형태로 우선순위를 빠르게 비교할 수 있어야 한다
- 증강체 이름, 태그, 설명 기반 검색을 제공해야 한다
- 증강체 보상표를 통해 스테이지/조건별 보상을 확인할 수 있어야 한다
- 운영 방향별 증강체 선택 흐름을 확인할 수 있어야 한다
  - Fast 8 운영
  - 리롤 운영
  - 유연한 전환 운영

#### 챔피언 가이드

- 챔피언별 이름, 코스트, 역할, 포지션, 시너지를 확인할 수 있어야 한다
- 챔피언 카드 hover 시 핵심 스탯을 빠르게 확인할 수 있어야 한다
  - 체력, 공격력, 방어력, 마법 저항력, 공격 속도, 사거리, 마나
- 챔피언별 3신기 아이템을 한 번에 확인할 수 있어야 한다
- 챔피언 상세 모달에서 스탯, 시너지, 추천 아이템을 다시 확인할 수 있어야 한다
- 챔피언은 코스트 기준으로 필터링할 수 있어야 한다
- 챔피언 즐겨찾기 또는 최근 확인한 가이드를 통해 자주 보는 정보를 빠르게 다시 열 수 있어야 한다

#### 데이터 요구사항

- 시너지, 아이템, 증강체, 챔피언 기본 정보는 Community Dragon JSON 기준으로 구성한다
- 챔피언, 아이템, 시너지 이미지는 Community Dragon CDN 이미지를 사용한다
- 아이템 승률 통계는 `GET /tft/match/v1/matches/{matchId}` 응답을 수집해 계산할 수 있어야 한다
- API 연동 전에는 실제 화면 흐름을 확인할 수 있도록 샘플 데이터 기반 UI를 제공한다
- API 연동 후에는 로딩, 빈 데이터, 에러 상태를 사용자에게 명확히 안내해야 한다

#### 상태 및 접근성 요구사항

- 탭 전환 시 사용자가 입력한 검색어와 필터 상태는 의도한 기준에 맞춰 유지 또는 초기화되어야 하며, 기준은 구현 코드에서 일관되게 적용해야 한다
- 검색어, 정렬, 코스트 필터, 즐겨찾기 필터 변경 시 페이지는 1페이지로 초기화되어야 한다
- 즐겨찾기한 챔피언은 개별 해제할 수 있어야 하며, 즐겨찾기 보기 상태에서 전체 목록으로 돌아갈 수 있어야 한다
- 최근 확인한 가이드는 사용자가 실제로 클릭하거나 상세를 확인한 항목 기준으로 갱신되어야 한다
- 시너지 / 아이템 추천 챔피언 / 챔피언 카드 간 이동은 현재 탭과 검색 조건을 예측 가능하게 갱신해야 한다
- 탭은 `aria-selected`, 페이지네이션은 `aria-current`, 즐겨찾기 버튼은 `aria-pressed` 상태를 제공해야 한다
- 챔피언 상세 모달은 열릴 때 초점이 모달 내부로 이동하고, 닫힐 때 이전 트리거로 초점이 돌아와야 한다
- 이미지 로딩 실패 시 항목 유형에 맞는 fallback 이미지를 표시해야 한다

### 6. Community — 커뮤니티

#### 파티원 찾기

- 파티원 모집글 목록을 전체 / 랭크 / 일반 / 커스텀 탭으로 필터링할 수 있다
- 티어, 모드, 키워드로 모집글을 검색할 수 있다
- 모집글 작성 시 제목, 랭크 유형, 최소 티어, 모집 인원, 플레이 스타일 메모를 입력할 수 있다
- 각 모집글에서 모집 상태(모집 중 / 대기 중), 현재 인원, 마감 시간, 태그를 확인할 수 있다
- 참여 버튼으로 모집글에 참여 신청할 수 있다

#### 실시간 채팅

- 채널 목록(#일반, #덱 공략, #파티 모집, #질문 & 답변)을 확인하고 참여할 수 있다
- 채널별 현재 접속 인원 수를 확인할 수 있다
- 채널 내 실시간 메시지를 주고받을 수 있다
- 현재 서버 전체 온라인 유저 수를 확인할 수 있다

### 7. PatchNotes — 패치 노트

- 최신 패치 노트와 이전 패치 히스토리를 한 화면에서 확인할 수 있다
- 선택된 패치 버전에 따라 상단 요약 정보가 변경되어야 한다
  - 패치 버전
  - 적용일
  - 패치 설명
  - 전체 변경 건수
  - 핵심 영향 변경 건수
  - 상향 / 하향 항목 수
- 현재 선택된 패치의 대표 이미지를 표시해야 한다
  - 이미지는 Community Dragon CDN 또는 프로젝트 내 fallback 이미지를 사용한다
  - 이미지 로딩 실패 시 기본 패치 엠블럼 이미지로 대체한다
- 챔피언 / 시너지 / 아이템 / 증강체 / 시스템 변경사항을 카테고리별로 확인할 수 있다
- 변경사항 목록은 각 항목의 시각 정보를 함께 제공해야 한다
  - 챔피언 변경은 챔피언 이미지
  - 시너지 변경은 시너지 아이콘
  - 아이템 변경은 아이템 아이콘
  - 증강체 변경은 증강체 또는 카테고리 대표 아이콘
  - 시스템 변경은 패치 기본 이미지
- 변경사항 항목에는 다음 정보를 표시해야 한다
  - 카테고리
  - 변경 유형(상향 / 하향 / 조정 / 신규)
  - 영향도(높음 / 중간 / 낮음)
  - 대상 이름
  - 요약 설명
  - 태그
- 변경사항 항목의 "상세 보기"를 클릭하면 이전 수치와 변경 수치를 비교할 수 있어야 한다
- 패치 변경사항은 검색과 필터를 제공해야 한다
  - 챔피언, 아이템, 키워드 검색
  - 전체 / 챔피언 / 시너지 / 아이템 / 증강체 / 시스템 카테고리 필터
  - 전체 변경 / 상향 / 하향 / 조정 / 신규 유형 필터
  - 영향 높음만 보기 필터
- 검색어 또는 필터 변경 시 목록은 첫 페이지로 초기화되어야 한다
- 변경사항 목록은 한 페이지에 5개씩 표시해야 한다
- 페이지 번호는 한 번에 최대 5개까지만 표시하고 이후 페이지는 더보기 흐름으로 이동한다
- 히스토리 영역에서 이전 패치를 클릭하면 해당 버전의 패치 정보를 조회할 수 있어야 한다
  - 선택된 히스토리 항목은 active 상태로 표시한다
  - 선택한 패치 기준으로 상단 요약, 핵심 요약, 변경 목록, 카테고리 카운트가 함께 갱신된다
  - 히스토리 항목에도 대표 이미지를 표시한다
- 선택한 패치에 검색 조건과 필터 조건을 적용할 수 있어야 한다
- 조건에 맞는 변경사항이 없을 경우 빈 데이터 안내를 표시해야 한다

#### 데이터 요구사항

- 패치노트 데이터는 패치 버전 목록, 선택 패치 요약, 변경사항 목록, 변경 전/후 상세 비교 정보를 분리해 조회할 수 있는 구조여야 한다
- 패치 변경사항 조회 시 패치 버전, 검색어, 카테고리, 변경 유형, 영향도, 페이지 정보를 파라미터로 전달할 수 있어야 한다
- 카테고리별 변경 건수와 핵심 영향 변경 건수는 API 응답 또는 응답 데이터 기반 계산으로 갱신할 수 있어야 한다
- 챔피언, 아이템, 시너지, 증강체 이미지는 Community Dragon CDN key 기반으로 매핑할 수 있어야 한다
- 이미지 매핑 또는 로딩 실패 시 프로젝트 내 fallback 이미지를 사용해야 한다
- API 연동 전에는 샘플 데이터 기반으로 실제 화면 흐름을 확인할 수 있어야 한다
- API 연동 후에는 로딩, 빈 데이터, 에러, 재시도 상태를 사용자에게 명확히 안내해야 한다

#### 상태 및 접근성 요구사항

- 패치 버전, 검색어, 카테고리, 변경 유형, 영향 높음 필터가 바뀌면 현재 페이지와 펼쳐진 상세 비교 상태는 초기화되어야 한다
- 히스토리에서 이전 패치를 선택해도 동일한 검색/필터 UI를 사용할 수 있어야 한다
- API 오류 발생 시 기존 화면이 조용히 비는 대신 에러 메시지와 재시도 버튼을 제공해야 한다
- 히스토리 선택 버튼은 `aria-pressed`, 페이지네이션은 `aria-current`, 상세 보기 버튼은 `aria-expanded` 상태를 제공해야 한다
- 키보드 사용자가 검색, 필터, 히스토리, 상세 보기, 페이지네이션을 순서대로 탐색할 수 있어야 한다
- 이미지 로딩 실패가 반복되어도 레이아웃이 깨지지 않아야 한다

---

## 공통 기능

- 헤더: 로고, 주요 메뉴 네비게이션, Riot API 연동 상태 표시
- 사이드바: 메뉴 접기/펼치기, 현재 페이지 하이라이트
- Riot API 연동 상태: 현재 접속자 수, 대기열 표시
