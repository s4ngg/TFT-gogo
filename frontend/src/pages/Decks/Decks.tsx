import { AppLayout } from '../../components/layout'

function Decks() {
  return (
    <AppLayout>
      <div style={{ padding: '40px' }}>
        <h1>덱 목록</h1>
        <p>현재 패치 추천 덱 목록을 확인할 수 있습니다.</p>
      </div>
    </AppLayout>
  )
}

export default Decks
